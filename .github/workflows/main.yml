name: CI
on: [push]
jobs:
  job1:
    runs-on: self-hosted
    name: deploy magento to minikube local 3
    steps:
    - uses: actions/checkout@v2
    - name: Deploy to minikube
      run:
        make minikube
     
