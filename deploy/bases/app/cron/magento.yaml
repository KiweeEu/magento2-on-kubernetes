apiVersion: batch/v1
kind: CronJob
metadata:
  name: magento-cron
spec:
  schedule: '* * * * *'
  jobTemplate:
    metadata:
      creationTimestamp: null
    spec:
      template:
        metadata:
          labels:
            app: magento-cron
            k8s-app: magento
        spec:
          initContainers:
          - name: wait-for-web
            image: curlimages/curl:8.4.0@sha256:4a3396ae573c44932d06ba33f8696db4429c419da87cbdc82965ee96a37dd0af
            command: ["/bin/sh"]
            args:
            - -c
            - |
              while ! curl -kILSs --fail -XGET \
                -H "Host: $(CONFIG__DEFAULT__WEB__COOKIE__COOKIE_DOMAIN)" \
                $(MAGENTO_WEB_SERVICE_HOST):$(MAGENTO_WEB_SERVICE_PORT_HTTP)/health_check.php
              do echo "waiting for $(MAGENTO_WEB_SERVICE_HOST)"
              sleep 1
              done
            envFrom:
            - configMapRef:
                name: config
          containers:
          - name: magento-cron
            image: kiweeteam/magento2:2.4.6-p2-1@sha256:0034bf68e0dfe177df5b4d7060ee264b031d001168b3f87f428d1141dc9c7518
            command: ["/bin/sh"]
            args:
            - -c
            - |
              php bin/magento cron:run
            envFrom:
            - configMapRef:
                name: config
            - configMapRef:
                name: additional
            resources:
              limits:
                cpu: 500m
                memory: 4Gi
              requests:
                cpu: 50m
                memory: 1Gi
          restartPolicy: Never
  concurrencyPolicy: Forbid
  startingDeadlineSeconds: 600
  failedJobsHistoryLimit: 20
  successfulJobsHistoryLimit: 5
